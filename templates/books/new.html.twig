{% extends 'base.html.twig' %}

{% block title %}{{ 'pages.books.title_add' | trans }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="page-header">{{ 'pages.books.title_add' | trans }}</div>

        {{ form(form) }}
    </div>
{% endblock %}
